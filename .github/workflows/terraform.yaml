name: Building Infrastructure with Terraform

on:
  workflow_call:


jobs:
  terraform:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:

    - name: Checkout the code
      uses: actions/checkout@v3

    - name: Configure AWS ( OIDC )
      uses: aws-actions/configure-aws-credentials@v6.0.0
      with:
        role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
        aws-region: ap-south-1

    - name: Setting Up Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false

    - name: Terraform Init
      working-directory: ./terraform
      run: terraform init -reconfigure

    - name: Terraform Validate
      working-directory: ./terraform
      run: terraform validate

    - name: Terraform Plan
      working-directory: ./terraform
      run: terraform plan -out=tfplan

    - name: Terraform Destroy Old Resources
      working-directory: ./terraform
      run: |
        terraform destroy -target=aws_eks_node_group.private_nodes --auto-approve || true
        terraform destroy -target=aws_db_instance.mysql --auto-approve || true
        terraform destroy --auto-approve || true

    - name: Terraform Apply
      working-directory: ./terraform
      run: terraform apply --auto-approve
